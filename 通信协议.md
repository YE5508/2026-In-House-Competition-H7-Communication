
# 上位机—主控—从板通信协议说明

本文档用于上位机、主控板和底盘/机构从板之间的通信对接。主控与上位机通过 UART1（蓝牙调试链路）通信，主控与从板通过 CAN 总线通信。

## 1. 通信参数与角色

```text
上位机 <-> UART1/蓝牙 <-> 主控 STM32F405 <-> CAN1/CAN2 <-> 底盘、机构从板
```

- UART1：115200 baud，8 数据位、无校验、1 停止位（8N1）。
- CAN：1 Mbps，29 位扩展帧，数据帧，DLC 最大 8。
- CAN ID 第 3、4 位编码总线：
  - `01`：CAN1
  - `02`：CAN2
  - `03`：CAN3
- 当前硬件只有 CAN1/CAN2，CAN3 报文不发送。
- 所有多字节数值采用小端序（little-endian）。

## 2. UART/蓝牙帧格式

每帧格式如下：

| 字段 | 长度 | 内容 |
| --- | ---: | --- |
| 包头 | 1 字节 | 固定 `0xA5` |
| 原始数据 | 可变 | 按 bool、byte、short、int、float 顺序排列 |
| 校验和 | 1 字节 | 原始数据所有字节求和的低 8 位 |
| 包尾 | 1 字节 | 固定 `0x5A` |

原始数据类型按以下顺序排列：

```text
bool -> byte -> short -> int -> float
```

bool 按位打包，每 8 个 bool 占 1 个字节，最低位对应第 0 个 bool。

数据类型长度：

| 类型 | 长度 |
| --- | ---: |
| bool | 1/8 字节 |
| byte | 1 字节 |
| short | 2 字节 |
| int | 4 字节 |
| float | 4 字节 |

short、int、float 均采用小端序。

## 3. 上位机发送控制包

当前控制包由以下数据组成：

- 18 个 bool；
- 3 个 byte；
- 3 个 short；
- 1 个 float。

控制包原始数据布局：

| 顺序 | 类型 | 字段 |
| ---: | --- | --- |
| bool0～17 | bool | 底盘、机构动作控制 |
| byte0 | byte | `Ball_Grab` |
| byte1 | byte | `BigBlock_Grab` |
| byte2 | byte | `BigBlock_Put` |
| short0 | short | `CarVx` |
| short1 | short | `CarVy` |
| short2 | short | `CarVw` |
| float0 | float | `Angle` |

### bool 控制位

| 位号 | 字段 | 说明 |
| ---: | --- | --- |
| 0 | `Chassis_Reset` | 底盘复位 |
| 1 | `BigBlock_Enable` | 大地块机构使能 |
| 2 | `BigBlock_Reset` | 大地块机构复位 |
| 3 | `SkyBlock_Enable` | 天空块机构使能 |
| 4 | `SkyBlock_Reset` | 天空块机构复位 |
| 5 | `Ball_Enable` | 灵石机构使能 |
| 6 | `Ball_Reset` | 灵石机构复位 |
| 7 | `BigBlock_ReadyToGrab` | 大地块准备抓取 |
| 8 | `SkyBlock_Grab` | 天空块抓取 |
| 9 | `SkyBlock_Put` | 天空块放置 |
| 10 | `SkyBlock_Carry` | 天空块机械臂归位 |
| 11 | `SkyBlock_JawOpen` | 天空块夹爪开合 |
| 12 | `Ball_Carry` | 灵石机构取/放动作 |
| 13 | `Ball_Open` | 灵石夹爪开合 |
| 14 | `All_Examine` | 全部自检 |
| 15 | `Chassis_Enable` | 底盘使能请求 |
| 16 | `Chassis_LockAngle` | 底盘锁角 |
| 17 | `Chassis_Protect` | 底盘保护条件 |

实际底盘使能值为：

```c
Chassis_Enable = bool15 && bool17;
```

底盘位置模式 `0x01010203` 当前暂不发送。

## 4. CAN ID 编码规则

CAN 使用 29 位扩展 ID。

以 `0x01020201` 为例：

```text
01 02 02 01
|  |  |  |
|  |  |  +-- 操作代码
|  |  +----- 接收设备编号
|  +-------- CAN 路由编号
+----------- 发送设备编号
```

设备编号：

| 编号 | 设备 |
| ---: | --- |
| 1 | 主控 |
| 2 | 底盘 |
| 3 | 大地块机构 |
| 4 | 天空块机构 |
| 5 | 灵石机构 |

CAN 路由编号：

| 编号 | CAN 总线 |
| ---: | --- |
| 01 | CAN1 |
| 02 | CAN2 |
| 03 | CAN3 |

主控发送时，根据 ID 中的 CAN 路由编号选择发送队列：

- `01` 路由到 `CAN1_Txqueue`；
- `02` 路由到 `CAN2_Txqueue`；
- `03` 当前无 CAN3 硬件，直接丢弃；
- 其他值直接丢弃。

## 5. 主控发送给从板的 CAN 控制帧

所有 CAN 控制帧均为扩展数据帧。

| 设备 | 功能 | CAN ID | DLC | DATA |
| --- | --- | --- | ---: | --- |
| 底盘 | 使能 | `0x01010201` | 2 | `M`, 使能值 |
| 底盘 | 速度控制 | `0x01010202` | 6 | `CarVx`、`CarVy`、`CarVw` |
| 底盘 | 锁角 | `0x01010204` | 4 | `Angle` 小端 float |
| 底盘 | 复位 | `0x010102FF` | 2 | `R`, `S` |
| 大地块 | 使能 | `0x01010301` | 2 | `M`, 使能值 |
| 大地块 | 准备抓取 | `0x01010302` | 2 | `P`, `E` |
| 大地块 | 抓取 | `0x01010303` | 2 | `G`, `1` 或 `2` |
| 大地块 | 放置 | `0x01010304` | 2 | `P`, `1` 或 `2` |
| 大地块 | 复位 | `0x010103FF` | 2 | `R`, `S` |
| 天空块 | 使能 | `0x01010401` | 2 | `M`, 使能值 |
| 天空块 | 抓取 | `0x01010402` | 2 | `G`, `S` |
| 天空块 | 放置 | `0x01010403` | 2 | `P`, `S` |
| 天空块 | 机械臂归位 | `0x01010404` | 2 | `A`, `R` |
| 天空块 | 夹爪开合 | `0x01010405` | 2 | `R`, 开合值 |
| 天空块 | 复位 | `0x010104FF` | 2 | `R`, `S` |
| 灵石 | 使能 | `0x01010501` | 2 | `M`, 使能值 |
| 灵石 | 抓取/放置 | `0x01010502` | 2 | `G`, `B` |
| 灵石 | 特殊动作 | `0x01010503` | 2 | `P`, `B` |
| 灵石 | 气泵/开合 | `0x01010504` | 2 | `R`, 状态值 |
| 灵石 | 复位 | `0x010205FF` | 2 | `R`, `S` |
| 广播 | 全部自检 | `0x01020100` | 2 | `C`, `H` |

其中：

- `M`：模式/使能命令；
- `G`：抓取命令；
- `P`：放置或准备命令；
- `R`：复位、开合或气泵命令；
- `A`：机械臂归位命令；
- `S`、`E`、`B`、`H`：具体机构操作参数。

速度数据排列：

```text
DATA0~1：CarVx，小端 short
DATA2~3：CarVy，小端 short
DATA4~5：CarVw，小端 short
```

锁角数据排列：

```text
DATA0~3：Angle，小端 float
```

## 6. 从板发送给主控的 CAN 反馈帧

主控收到反馈帧后，根据 CAN ID 更新反馈状态结构体。

未知 CAN ID 不更新反馈状态。

| 设备 | 功能 | CAN ID | DLC | DATA |
| --- | --- | --- | ---: | --- |
| 底盘 | 使能反馈 | `0x02010101` | 2 | DATA1 为状态值 |
| 底盘 | 速度模式反馈 | `0x02010102` | 2 | DATA1 为状态值 |
| 底盘 | 位置模式反馈 | `0x02010103` | 2 | DATA1 为状态值 |
| 底盘 | Yaw 反馈 | `0x020101AA` | 4 | DATA0～3 为小端 float |
| 底盘 | 错误反馈 | `0x020101EE` | 4 | DATA0～3 为错误原始字节 |
| 底盘 | 复位反馈 | `0x020101FF` | 2 | DATA1 为复位结果 |
| 大地块 | 使能反馈 | `0x03010101` | 2 | DATA1 为状态值 |
| 大地块 | 错误反馈 | `0x030101EE` | 4 | DATA0～3 为错误原始字节 |
| 大地块 | 复位反馈 | `0x030101FF` | 2 | DATA1 为复位结果 |
| 天空块 | 使能反馈 | `0x04010101` | 2 | DATA1 为状态值 |
| 天空块 | 错误反馈 | `0x040101EE` | 4 | DATA0～3 为错误原始字节 |
| 天空块 | 复位反馈 | `0x040101FF` | 2 | DATA1 为复位结果 |
| 灵石 | 使能反馈 | `0x05010101` | 2 | DATA1 为状态值 |
| 灵石 | 抓取反馈 | `0x05010102` | 2 | DATA1 为状态值 |
| 灵石 | 放置反馈 | `0x05010103` | 2 | DATA1 为状态值 |
| 灵石 | 气泵反馈 | `0x05010104` | 2 | DATA1 为状态值 |
| 灵石 | 错误反馈 | `0x050101EE` | 4 | DATA0～3 为错误原始字节 |
| 灵石 | 复位反馈 | `0x050101FF` | 2 | DATA1 为复位结果 |
| 底盘广播 | 状态反馈 | `0x02010100` | 2 | DATA1 为广播状态 |
| 机构广播 | 状态反馈 | `0x01010100` | 2 | DATA1 为广播状态 |

Yaw 数据：

```text
Yaw = little_endian_float(DATA0, DATA1, DATA2, DATA3)
```

错误帧中的 4 个字节不在主控端解释，直接保存和转发。

## 7. 主控回传上位机的蓝牙反馈包

主控约每 10 ms 发送一次反馈快照。

反馈包格式仍为：

```text
0xA5 + 原始数据 + 校验和 + 0x5A
```

反馈原始数据固定为：

- 31 个 byte；
- 1 个 float。

### byte 字段顺序

| 偏移 | 字段 |
| ---: | --- |
| 0 | 底盘使能 |
| 1 | 底盘速度模式 |
| 2 | 底盘位置模式 |
| 3 | 底盘复位 |
| 4～7 | 底盘错误原始数据 |
| 8 | 大地块使能 |
| 9～12 | 大地块错误原始数据 |
| 13 | 大地块复位 |
| 14 | 天空块使能 |
| 15～18 | 天空块错误原始数据 |
| 19 | 天空块复位 |
| 20 | 灵石使能 |
| 21 | 灵石抓取状态 |
| 22 | 灵石放置状态 |
| 23 | 灵石气泵状态 |
| 24～27 | 灵石错误原始数据 |
| 28 | 灵石复位 |
| 29 | 底盘广播状态 |
| 30 | 机构广播状态 |

### float 字段

| 字段 | 说明 |
| --- | --- |
| float0 | 底盘 Yaw，小端 float |

上位机必须按照上述固定顺序解析，不得根据当前字段是否有效改变偏移。

## 8. 任务调度

主控 CAN 发送任务为 `CAN_SendcmdTask`。

任务行为：

- 每 1 ms 尝试发送一帧 CAN1 队列数据；
- 每 1 ms 尝试发送一帧 CAN2 队列数据；
- 每约 10 ms 发送一次蓝牙反馈快照；
- UART 忙时等待下一周期发送；
- CAN 发送失败时保留队首帧，下一周期继续重试。

## 9. 对接流程

1. 上位机按照控制包字段顺序组装原始数据。
2. 对原始数据求和，取低 8 位作为校验和。
3. 添加包头 `0xA5` 和包尾 `0x5A` 后发送。
4. 主控解析控制包并生成 CAN 控制帧。
5. 主控根据 CAN ID 第 3、4 位将报文路由到 CAN1 或 CAN2。
6. 从板按照反馈 ID 发送状态、Yaw、错误和复位反馈。
7. 主控解析反馈并生成固定格式蓝牙反馈包。
8. 上位机按照固定字段顺序解析反馈包。

## 10. 当前限制

- 底盘位置模式下行 `0x01010203` 暂不发送。
- 当前 STM32F405 工程无 CAN3 硬件，CAN 路由为 `03` 的报文会丢弃。
- CAN 队列暂未加入并发保护。
- 蓝牙接收错误重发机制暂不实现。
- 错误字段仅按原始字节转发，具体错误码含义由从板协议约定。
- 实际 CAN 反馈 DLC 和各 DATA 字节含义仍需使用 CAN 分析仪与从板固件联调确认。
